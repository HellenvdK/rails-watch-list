<div class="container">
  <h2>These are the lists🍿🎬</h2>

  <% @lists.each do |list| %>
  <div class="card">
    <%= link_to list.name, list_path(list) %>
  </div>
  <% end %>
</div>

 <%= link_to "Create a list", new_list_path %>

